# 📝 Lista de Tarefas (To-Do List)

> Projeto desenvolvido para praticar os fundamentos do JavaScript, manipulação do DOM e persistência de dados.

## 💻 Sobre o Projeto

Este é um aplicativo web simples e eficiente para gerenciamento de tarefas diárias. O objetivo principal do desenvolvimento foi criar uma aplicação funcional utilizando **apenas JavaScript Puro (Vanilla JS)**, sem depender de frameworks, para consolidar o entendimento sobre a lógica de programação web.

## ✨ Funcionalidades

* **Adicionar Tarefas:** Permite inserir novas tarefas na lista.
* **Validação:** Impede a criação de tarefas vazias.
* **Marcar como Concluído:** Ao clicar no texto, a tarefa é riscada (toggle).
* **Excluir Tarefas:** Botão de lixeira para remover itens da lista.
* **Persistência de Dados:** Uso do **LocalStorage** para que as tarefas não sumam ao atualizar a página (F5).

## 🛠️ Tecnologias Utilizadas

* **HTML5:** Estrutura semântica.
* **CSS3:** Estilização da interface.
* **JavaScript (ES6+):** Lógica da aplicação.

## 🧠 Aprendizados

Durante a construção e no processo de aprendizagem deste projeto, foram aplicados e vistos os seguintes conceitos:

* **Manipulação do DOM:** Seleção de elementos (`getElementById`), criação de HTML dinâmico (`innerHTML`) e alteração de classes CSS.
* **Eventos:** Uso de `addEventListener` e o conceito de **Event Delegation** (escutar eventos na lista pai para afetar os botões filhos).
* **Arrays e Objetos:** Manipulação de estruturas de dados com métodos como `.push()`, `.forEach()`, `.filter()` e `.find()`.
* **JSON & LocalStorage:** Conversão de dados (`JSON.stringify` / `JSON.parse`) para salvar e ler informações do navegador.
* **Lógica de Programação:** Tratamento de erros, condicionais e prevenção de comportamento padrão (`preventDefault`).

## 🚀 Como rodar o projeto

1.  Clone este repositório ou baixe os arquivos.
2.  Abra o arquivo `index.html` em qualquer navegador moderno.
3.  Pronto! A aplicação já está rodando (não requer instalação de dependências).

---

Desenvolvido por Estênio Gomes Dantas
